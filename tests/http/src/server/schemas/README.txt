Only files ending with '.schema.json' are processed.
Only requests matching '/meta/apiMethods/METHOD: true' are handled in requests.
Those files must contain a valid json object with a '$id' at depth 1.

